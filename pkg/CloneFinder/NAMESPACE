###
### NAMESPACE
###

### convert base functions or S3 methods into S4 methods
import(methods)
importFrom("graphics", "plot", "hist",
           "abline", "lines", "par", "points", "segments", "text")
exportMethods("plot", "hist") # come from 'graphics'
exportMethods("summary")      # comes from 'base'

### other imported methods (to pacify R CMD check)
importFrom("gtools", "rdirichlet", "ddirichlet")

importFrom("utils", "data")
importFrom("grDevices", "colorRampPalette", "dev.off", "pdf") # really???
importFrom("stats",
           "rbinom", "cov", "cutree", "dbeta", "dbinom", "density",
           "dgamma", "dgeom", "dist", "dnorm", "hclust", "median",
           "na.omit", "optim", "pnorm", "rbeta", "rect.hclust",
           "rgamma", "rgeom", "rmultinom", "rnbinom", "rnorm", "runif",
           "sd", "var")

### dealing with simplices
importFrom(combinat, "combn")    # used for combinations of psi-values
importFrom(combinat, "xsimplex") # used for generateSimplex
export("sampleSimplex", "generateSimplex")

### weight vectors
exportClasses("WeightVector")
export("WeightVector")

### tumors
exportClasses("Tumor")
export("Tumor", "getClone")
export("tumorGen") # deprecated

### needed for simulations
exportClasses("Clone")
export("Clone")
export("generateData", "trueZ", "sizeplot")

### prefit (phi vectors per segment)
exportClasses("PrefitCloneModel")
export("PrefitCloneModel")

### posterior phi
export("posteriorPhi", "posteriorRegion", "posteriorQuantile", "plotPosteriorCDF")

### fitting psi vectors
export("guessPsi", "runEMalg")

### algorithm.R
importFrom("sirt", "dirichlet.mle")
export("psiOptim", "runAlg", "psiFilter")
### simgen.R
export("dataGen", "plot.data")
### functions.R

